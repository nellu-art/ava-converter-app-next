import { Content } from '@/app/converter/_components/Content'

export default async function MainPage() {
  return (
    <main className="h-[100svh] flex flex-col items-center">
      <Content />
    </main>
  )
}
